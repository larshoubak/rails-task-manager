<h2><%= @task.title %></h2>

<p><%= @task.details%></p>

<% if @task.completed %>
  <p>
    Completed!
  </p>
<% else %>
  <p>
    This task is not completed yet.
  </p>
<% end %>

<%= link_to 'Edit this task', edit_path %>

<%= link_to 'Back to list', tasks_path %>
